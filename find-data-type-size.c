#include <stdio.h>

int main () {
    // declare variable 

    int i;
    float f;
    double d;
    char c;

    // output
    printf("Size of integer data: %d\n", sizeof(i));
    printf("Size of float data: %d\n", sizeof(f));
    printf("Size of double data: %d\n", sizeof(d));
    printf("Size of char data: %d\n", sizeof(c));

    return 0;
}